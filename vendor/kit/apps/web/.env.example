# Must match the host that returns HTTP 200 without redirect.
# Production currently serves www; apex (agentcn.dev) 307s to www and breaks some OG crawlers.
NEXT_PUBLIC_APP_URL=https://www.agentcn.dev
